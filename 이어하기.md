---
title: 사장으로 살아남기 — 이어하기 (세션 인수인계)
category: 게임기획
tags: [토이게임, boss, MBTI, 밸런스, UI, 인수인계]
summary: 2026-09-11~12 세션에서 한 일과 확정된 설계 결정. 다음 세션은 이 문서부터 읽는다
created: 2026-09-12
updated: 2026-09-12
---

# 사장으로 살아남기 — 이어하기

> 생성: `/summarize` 스킬로 세션 작업 정리 (2026-09-12 10:30)

## 0. 지금 상태 한 줄

**플레이 가능한 게임이 됐다.** `open index.html` 하면 바로 돌아간다.
`node sim.js` → **정합성 13종 + 종합 판정 26/26 통과**.

```yaml
커밋:   cb8e37e "게임 본체 완성 — UI · 이벤트 15개 · 밸런스 재설계"  (main, push 완료)
미커밋: index.html · story.js  — 사무실 SVG · 애니메이션 · 말풍선 · sticky 레이아웃
        (동작·검증 다 끝남. 커밋만 안 한 상태)
```

---

## 1. 파일 구조

| 파일 | 역할 | 누가 고치나 |
|---|---|---|
| `index.html` | `<script>` 2개. **①로직 ②화면** | 클로드 |
| `story.js` | 이벤트 15개 · 선택지 49개 · 슬롯 대사 8개 ★런타임 원본 | 가민지가 쓰고 클로드가 옮김 |
| `sim.js` | 시뮬레이터. 정합성 13종 + 판정 26개 | 클로드 |
| `치트시트.js` → `치트시트.md` | MBTI 치트시트 자동 생성 | 자동 |
| `기획.md` | 현행 사양 SSOT | 클로드 |
| `이벤트_작성규칙.md` | 이벤트 작성 규칙 SSOT | 가민지 |
| `이벤트.txt` · `기획.txt` | 초안 보관. **런타임은 안 읽는다** | 가민지 |

```yaml
★ index.html 의 <script> 는 순서가 중요하다:
   ① 첫 블록 = 로직.  sim.js 가 vm 으로 구동하는 유일한 블록
   ② 둘째 블록 = 화면. 그리기만 한다
   로직을 화면 블록에 두면 검증 밖으로 샌다
```

---

## 2. 확정된 설계 — 되돌리지 말 것

각 항목은 **실제로 터진 문제를 고친 결과**다. 되돌리면 그 문제가 재발한다.

### 2-1. 승급 = 기간 + 직원 + 분기매출 (셋 다)

| 단계 | 기간 | 직원 | 분기매출 | 제때 가면 | 진단서 |
|---|--:|--:|--:|--:|:-:|
| 중소기업 | 진입 +16분기 | 50명 | 7억 | 4년 | ① |
| 중견기업 | 진입 +16분기 | 100명 | 15억 | 8년 | ② |
| 대기업 | 진입 +16분기 | 150명 | 22억 | 12년 | ③ **엔딩** |

```yaml
기간은_상대다: "그 단계에 들어온 뒤 N분기". 절대 분기가 아니다.
  절대값(16/32/48)으로 뒀더니 직원 50명을 못 채워 84분기까지 스타트업에 머문 판이
  드디어 채웠을 때 중견 조건까지 동시에 만족 → 21년차 중소, 22년차 중견 연속 승급.
  진단서가 같은 유형(INFJ)으로 두 장 나와 기획 §8 "변천사" 가 죽었다

매출_문턱이_높은_이유: 9억으로 뒀더니 직원 106명에서 이미 20억이라 두 배를 넘겨
  "매출을 더 올릴 이유" 가 사라졌다. 플레이어 말: "안 늘려도 돈이 벌려서 누를 필요가 없었어"
  문턱을 직원 문턱에서 겨우 닿는 수준으로 올리면 매출 = 직원수 × … 이므로
  "매출을 늘리는 유일한 길이 채용" 이 된다

단계는_승급으로만_오른다: S.단계 로 들고 간다. 직원 수로 되돌아 계산하면
  승급을 안 거치고 올라가 진단서가 사라진다 (sim.js 정합성 2번이 고정)
```

### 2-2. 이벤트는 "단계 진입 후 N분기" 에 확률 없이 뜬다

```
스타트업   +1 · +3 · +5 · +7 분기    → [5]  승급 조건 충족 시
중소기업   +1 · +5 · +9 · +13 분기   → [10] 승급 조건 충족 시
중견기업   +1 · +5 · +9 · +13 분기   → [15] 승급 조건 충족 시
```

```yaml
확률을_뺀_이유: 0.7/분기로 뽑았더니 단계 앞쪽에 몰려 터지고 뒤가 텅 비었다
  (실측 이벤트 사이 최대 공백 중앙값 12분기 = 3년)
절대분기가_아닌_이유: 늦게 승급하면 다음 단계 이벤트 4개가 한꺼번에 풀려 또 몰린다
금액기준이_아닌_이유: 누적 매출이 플레이 스타일에 따라 1.4~2.0배 차이.
  외주는 I·S, 영업은 E·S 라 이벤트 진행 속도가 성격에 따라 달라진다 (MBTI 게임에 치명적)
스타트업만_촘촘한_이유: 이벤트4 가 인력관리를 해금한다. 16분기까지 채용 5회를 해야
  승급하므로 해금이 늦으면 시간이 모자란다
```

### 2-3. 그 외 확정 사항

| 항목 | 값 | 왜 |
|---|---|---|
| 자연 회복 | 목표 35 · 아래 0.07 / 위 0.04 | 죽음의 나선 차단. **목표를 안전구간(40~75) 안에 두면 영업이 열등 선택지가 된다** |
| 영업 | 평판 75 미만 +8, 이상 **−6** | 무리한 영업은 역효과. E 성향에 과열 해소 수단을 준다 |
| 외주 | 평판 10 미만이면 **잠김** | 경고문("일감이 안 들어옵니다")과 버튼이 어긋나면 안 된다 |
| 채용 | 실력 **희석** (경력 55 / 신입 25 가 평균에 섞임) | 기획 §3 "실력 = 팀 평균. 신입 뽑으면 떨어짐" 을 구현. 안 하면 과열 |
| 채용·해고 | 평판 영향 **없음** | 넣었더니 평판 과열 0.1%→24.5%, "평판 낮추려 사람 자르기" 발생 |
| 고정 운영비 | 800만/분기, 매년 15% | 직원 수에 비례하지 않는 **유일한** 비용. 없으면 "작아도 괜찮다" 가 성립 |
| 인력관리 4버튼 | 이벤트4 전까지 잠김 | 혼자인 회사에 인력관리는 없다 |
| 기본매출 | 2,200만 | 직원 10배 스케일에서 1,550 이면 마진 9% 라 적자 40% |

---

## 3. 검증 방법

```bash
cd ~/study/toy/boss

node sim.js            # 정합성 13종 + 판정 26개.  전부 ✓ 여야 한다
node 치트시트.js        # 치트시트.md 재생성 (축·선택지 고쳤으면 필수)
open index.html        # 실제 플레이
```

```yaml
★ node sim.js 는 첫 <script>(로직)만 읽는다 — 화면 블록의 문법 오류를 못 잡는다.
  UI 를 건드렸으면 반드시 따로 검사할 것 (INDEX §6 "브라우저 띄우기 전에 JS 구문검사"):

  python3 -c "
  import io,re
  s=io.open('index.html',encoding='utf-8').read()
  for i,b in enumerate(re.findall(r'<script>([\s\S]*?)</script>', s)):
      io.open('/tmp/b%d.js'%i,'w',encoding='utf-8').write(b)
  "
  node --check /tmp/b0.js && node --check /tmp/b1.js
```

### 헤드리스 UI 테스트

이 맥엔 브라우저 자동화 도구가 없고 Safari 뿐이라, **최소 DOM 셰임**으로 UI 를 구동했다.
셰임은 세션 스크래치패드에 있었으므로 다음 세션에서는 다시 만들어야 한다.

```yaml
필요한_DOM_표면: getElementById(트리 검색) · createElement · className · textContent
                 innerHTML(간단 파싱) · appendChild · classList · onclick · disabled · style
추가로_필요: setTimeout / clearTimeout  (말풍선이 쓴다. 큐에 쌓아뒀다 수동 발화)
검증한_흐름: 타이틀 → 창업 → 배치 → 진행 → 이벤트 → 승급 → 저장/이어하기
```

---

## 4. 이번 세션에 밟은 함정 11개

`기획.md` §14 에 전문이 있다. 요약:

| # | 함정 | 교훈 |
|:-:|---|---|
| 1 | 죽음의 나선 (I·S 가 평판 2 에서 영구 적자) | 3중곱 계수는 되돌릴 수단이 있어야 한다 |
| 2 | 회복목표를 안전구간 안에 두니 영업이 죽음 | **해결책이 새 결함을 만든다** |
| 3 | T/F 표본 부족 (192판 중 152판 유보) | 기획서가 경고해둔 걸 그대로 밟았다 |
| 4 | **시뮬레이터 시드 버그** (16유형이 2판이었다) | 게임 의심 전에 **측정 도구를 의심**하라 |
| 5 | 채용에 지불 능력 검사 없음 | UI 나오면 즉시 악용될 구멍 |
| 6 | 해고 보상이 정액 | 규모 비례 여부를 항상 확인 |
| 7 | 승급이 단계를 건너뛰면 진단서 소실 | |
| 8 | 돈을 풀었더니 대기업이 뚫림 | **또 한 번, 해결책이 새 결함을** |
| 9 | 10배 스케일에서 마진 소멸 | |
| 10 | 회사를 키울수록 지표 과열 | 기획서에 답이 적혀 있었다 (실력=팀 평균) |
| 11 | **플레이어 모델이 설계를 못 따라감** | **규칙을 바꾸면 플레이어 모델도 바꿔라** |

```yaml
★ 측정_도구_함정이_3회: 시드 버그(4) · 승급 기준 변경(11) · 매출 문턱 변경 후 재발(11)
  "게임이 아니라 모델을 재고 있지 않은가" 를 매번 확인할 것
★ UI_전용_버그가_2회: const 이름 충돌(CFG·스토리) — 브라우저에서 페이지가 통째로 죽는다.
  sim.js 가 못 잡으므로 node --check 필수
```

---

## 5. 남은 작업

### 바로 할 수 있는 것

```yaml
미커밋_커밋: index.html · story.js (SVG·말풍선·sticky). 동작 검증 끝남
  ※ 토이게임은 브랜치 없이 main 에 바로 커밋한다 (2026-09-12 확정)
```

### 콘텐츠 (가민지 영역)

| 항목 | 상태 |
|---|---|
| 위기 이벤트 | **0개.** 규칙 §1 의 4종 중 유일하게 안 쓴 것 (돈 바닥·분위기 최악) |
| 이벤트15 축·결과 | 클로드 초안 (T/J/F/P). `story.js` 에 `초안: true` 표시됨 |
| 16유형 결과 문구 | 미작성. 지금은 4글자만 뜬다 |
| 그림 | SVG 로 대체됨. 직접 그리려면 `.stage-art` 안을 `<img>` 로 교체 |

### 기능

| 항목 | 메모 |
|---|---|
| 카카오 공유 | 기획 §11. 지금은 클립보드 폴백만. 앱 등록은 가민지 계정으로 |
| 진행 연출 | 기획 §13 `자동흐름_속도` 미정. 지금은 4분기가 한 틱에 끝난다 (팝업 없으면 즉시) |
| 배속 버튼 | 위와 세트 |
| 투자자 유치 | 기획 §13. 빚을 받아줄 정상 경로 |

### 밸런스 관찰 대상

```yaml
E_계열_적중률: I 계열 100% 인데 E 계열 78~90% (ESFJ/ESFP 78%).
  영업 배수 0.2 · 마케팅 0.5 라 매출이 덜 나와 승급이 늦고, 이벤트를 덜 봐서
  T/F 표본이 준다. 목표(70%)는 넘지만 22%p 차이.
  손대려면 영업·마케팅 배수를 조금 올리는 게 후보

작은_회사로_버티기: 고정 운영비를 넣어도 채용을 아예 안 하면
  12년을 3명으로 버틸 수 있다 (3.6억 흑자). 더 막으려면 적자로 만들어야 하는데
  그러면 승급을 노리는 사람도 같이 조여 게임이 부서진다 (2,000만·18% 에서 엔딩 91%→2%)
  지금은 "안 키우면 승급 못 한다" 는 목표 압박으로만 잡혀 있다
```

---

## 6. 세로 레이아웃 예산 (UI 손댈 때 참고)

회사 탭은 **사무실 + 배치판이 고정, 카드만 스크롤**이다.

| 영역 | 높이 |
|---|--:|
| 상태바 `#bar` (+부족 힌트) | 188px |
| 사무실줄 `.artrow` | 121px |
| 배치판 `.plan` | 140px |
| 구분선 | 10px |
| 탭바 `#tabs` | 41px |
| **고정 합계** | **500px** |

| 기기 | 카드 영역 | 카드 필요 |
|---|--:|--:|
| iPhone SE (667) | 167px | 292px |
| 갤럭시 S (800) | 300px | 292px ✓ |

```yaml
더_줄이려면: 게이지 3개를 한 줄로(-34) · 승급 칩 접기(-30) · 건물 높이 104→80(-24)
건물은_오른쪽_말풍선은_왼쪽: 겹쳐 띄우면 세로를 더 먹고 스크롤 시 말풍선이 화면 밖으로 나간다
말풍선: width:fit-content + margin-left:auto — 대사 길이만큼만 차지하고 건물 쪽에 붙는다
```

---

## 7. 다음 세션 시작 순서

```yaml
1. 이 문서를 읽는다
2. node sim.js  → 26/26 인지 확인 (아니면 뭔가 어긋난 것)
3. open index.html  → 실제로 돌아가는지
4. 미커밋분 커밋 (5절)
5. 방법론이 필요하면 ~/study/toy/00_게임제작_INDEX.md 가 라우팅한다 (통독 금지)
```

```yaml
관련_문서:
  기획.md              현행 사양 SSOT. §14 에 함정 11개 전문
  이벤트_작성규칙.md    이벤트 쓸 때 옆에 띄워두는 규칙
  치트시트.md          어떤 선택이 어느 축으로 가는지 (자동 생성)
  00_게임제작_INDEX.md  방법론 라우팅. boss 는 아직 §0 표에 미등재
```
